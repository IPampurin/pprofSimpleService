version: '3.9'

services:
  
  pprof-simple-service:
    build: .
    container_name: pprofSimple
    ports:
      - ${HTTP_PORT}:${HTTP_PORT}
    env_file:
      - .env
    networks:
      - simple-network

networks:
  simple-network:
    driver: bridge